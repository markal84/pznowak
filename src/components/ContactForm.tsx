'use client';
import ArrowIcon from '@/components/ArrowIcon';
import {useState,useEffect} from 'react';
export default function ContactForm(){const [project,setProject]=useState('');const [ready,setReady]=useState(false);useEffect(()=>{setProject(new URLSearchParams(window.location.search).get('projekt')||'')},[]);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const subject=project?`Zapytanie o pierścionek ${project}`:'Indywidualny projekt biżuterii';const body=`Dzień dobry,\n\n${d.get('message')}\n\n${d.get('name')||''}\nTelefon: ${d.get('phone')||'nie podano'}`;window.location.href=`mailto:kontakt@pznowak.pl?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;setReady(true)}return <form className="contact-form" onSubmit={submit}><h2>{project?`Porozmawiajmy o ${project}`:'Opowiedz o swoim pomyśle.'}</h2><p>Przygotuj wiadomość i otwórz ją w swojej aplikacji pocztowej. Tam sprawdzisz treść i wyślesz ją do pracowni.</p><label htmlFor="name">Imię (opcjonalnie)</label><input id="name" name="name" autoComplete="given-name"/><label htmlFor="phone">Telefon (opcjonalnie)</label><input id="phone" name="phone" type="tel" autoComplete="tel"/><label htmlFor="message">Twój pomysł lub pytanie</label><textarea id="message" name="message" required minLength={10} rows={5} placeholder="Dla kogo powstaje biżuteria? Jaki styl Ci się podoba? Jeśli masz ważny termin, wspomnij o nim."/><button className="button" type="submit">Otwórz wiadomość w poczcie <span aria-hidden="true"><ArrowIcon /></span></button><p className="form-note">Możesz też napisać bezpośrednio na <a style={{textDecoration:'underline'}} href="mailto:kontakt@pznowak.pl">kontakt@pznowak.pl</a>.</p>{ready&&<p role="status" className="form-note">Wiadomość nie została jeszcze wysłana. Dokończ ją w swojej poczcie. Jeśli aplikacja się nie otworzyła, skopiuj treść i użyj adresu powyżej.</p>}</form>}
