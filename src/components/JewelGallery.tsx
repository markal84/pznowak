'use client';
import {useState} from 'react';
import Lightbox from 'yet-another-react-lightbox';
import Zoom from 'yet-another-react-lightbox/plugins/zoom';
import 'yet-another-react-lightbox/styles.css';
export default function JewelGallery({images,name,video}:{images:string[];name:string;video?:string}){const [active,setActive]=useState(0);const [open,setOpen]=useState(false);return <div><button className="gallery-main" onClick={()=>setOpen(true)} aria-label={`Powiększ zdjęcie pierścionka ${name}`}><img src={images[active]} alt={`${name} — ujęcie ${active+1}`} width="700" height="700"/><span className="zoom-label">Powiększ zdjęcie +</span></button><div className="thumbnails" role="group" aria-label="Zdjęcia pierścionka">{images.map((src,i)=><button key={src} onClick={()=>setActive(i)} aria-pressed={i===active} aria-label={`Pokaż ujęcie ${i+1}`}><img src={src} alt="" width="80" height="80"/></button>)}</div>{video&&<details className="product-info"><summary>Zobacz pierścionek w ruchu</summary><video controls preload="none" playsInline poster={images[0]} style={{width:'100%',maxHeight:600}} aria-label={`Film — pierścionek ${name}`}><source src={video} type="video/mp4"/>Twoja przeglądarka nie obsługuje odtwarzania filmu.</video></details>}<Lightbox open={open} close={()=>setOpen(false)} index={active} slides={images.map((src,i)=>({src,alt:`${name} — ujęcie ${i+1}`}))} plugins={[Zoom]} labels={{Close:'Zamknij',Next:'Następne zdjęcie',Previous:'Poprzednie zdjęcie','Zoom in':'Powiększ','Zoom out':'Pomniejsz'}}/></div>}
