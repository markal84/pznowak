{
  "name": "pznowak-catalog-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check api/health.js && node --test"
  }
}
